<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Grow a Garden - Loading</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;400&display=swap" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }
    body {
      min-height: 100vh;
      font-family: 'Montserrat', Arial, sans-serif;
      background: linear-gradient(135deg, #0f2027 0%, #2c5364 100%);
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    /* Decorative animated gradient circles */
    .circle {
      position: absolute;
      border-radius: 50%;
      opacity: 0.45;
      filter: blur(8px);
      z-index: 0;
      animation: float 12s infinite alternate;
    }
    .circle1 {
      width: 350px; height: 350px;
      background: radial-gradient(circle at 60% 40%, #ffecd2 0%, #fcb69f 80%);
      top: 5%; left: 7%;
      animation-delay: 0s;
    }
    .circle2 {
      width: 220px; height: 220px;
      background: radial-gradient(circle at 60% 40%, #43cea2 0%, #185a9d 90%);
      bottom: 10%; right: 10%;
      animation-delay: 2s;
    }
    .circle3 {
      width: 150px; height: 150px;
      background: radial-gradient(circle at 30% 70%, #fffde4 0%, #005aa7 95%);
      top: 65%; left: 70%;
      animation-delay: 4s;
    }
    @keyframes float {
      0% { transform: translateY(0) scale(1);}
      100% { transform: translateY(-20px) scale(1.08);}
    }
    .loading-container {
      background: rgba(30, 30, 30, 0.93);
      border-radius: 18px;
      padding: 48px 32px 40px 32px;
      box-shadow: 0 8px 32px rgba(0,0,0,.32);
      text-align: center;
      width: 400px;
      max-width: 92vw;
      position: relative;
      z-index: 2;
    }
    .title {
      font-size: 2.4em;
      font-weight: 700;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 14px;
      letter-spacing: 2px;
      margin-bottom: 5px;
    }
    .subtitle {
      font-size: 2.4em;
      font-weight: 700;
      color: #fff;
      letter-spacing: 2px;
      margin-bottom: 26px;
      margin-top: -18px;
    }
    .emoji {
      font-size: 2.1em;
    }
    .loading-text {
      color: #eaeaea;
      font-size: 1.05em;
      margin-bottom: 16px;
      font-weight: 400;
    }
    .bar-bg {
      background: #fff;
      border-radius: 12px;
      width: 100%;
      height: 32px;
      margin: 0 auto 16px auto;
      position: relative;
      box-shadow: 0 1px 5px rgba(0,0,0,0.09);
      overflow: hidden;
      text-align: left;
    }
    .bar-fill {
      background: linear-gradient(90deg, #43cea2 0%, #185a9d 100%);
      height: 100%;
      border-radius: 12px 0 0 12px;
      width: 0;
      transition: width 0.25s;
      position: absolute;
      top: 0; left: 0;
      z-index: 1;
    }
    .percent-text {
      color: #fff;
      font-size: 1.7em;
      font-weight: 700;
      margin-top: 8px;
      letter-spacing: 3px;
      text-shadow: 0 2px 8px rgba(0,0,0,0.14);
    }
  </style>
</head>
<body>
  <div class="circle circle1"></div>
  <div class="circle circle2"></div>
  <div class="circle circle3"></div>

  <div class="loading-container">
    <div class="title">
      <span class="emoji">🌴</span>
      GROW A
      <span class="emoji">🌴</span>
    </div>
    <div class="subtitle">GARDEN</div>
    <div class="loading-text">Script Loading Please Wait for a While</div>
    <div class="bar-bg">
      <div class="bar-fill" id="barFill"></div>
    </div>
    <div class="percent-text" id="percentText">0%</div>
  </div>
  <script>
    let percent = 0;
    function animateLoading() {
      percent += Math.random() * 6 + 2; // random progression
      if (percent > 100) percent = 100;
      document.getElementById('barFill').style.width = percent + '%';
      document.getElementById('percentText').innerText = Math.floor(percent) + '%';
      if (percent < 100) {
        setTimeout(animateLoading, 400);
      } else {
        document.getElementById('percentText').innerText = 'Loaded!';
        // You can add code here to hide the loading screen or redirect
      }
    }
    animateLoading();
  </script>
</body>
</html>
